# 更新日志

[English](CHANGELOG.md) · **简体中文**

某个标签对应的小节会成为该版本在 GitHub 上的 release 说明,应用的更新弹窗里显示的也是同一段
文字。打标签前把新版本加到最上面,英文版 [CHANGELOG.md](CHANGELOG.md) 也要同步。

## 0.5.6

### 修复
- 手机返回键按主页面实际访问顺序返回，并保留各标签状态；先关闭侧滑操作、退出多选，再返回上一页面。详情页仍返回种子列表。
- 仪表盘实例卡准确打开对应服务器，包括从统一视图切入。
- 列表操作增加执行中、成功、失败反馈及重复请求保护；同时设置上传/下载限速不会漏发，旧操作不会误清新选择。
- 批量添加部分失败时保留失败链接/文件；有逐项失败明细时仅重试剩余项，提交期间锁定草稿避免误改。
- 隐私模式覆盖详情全部标签的名称、路径、哈希、Tracker、Peer、WebSeed 等敏感展示；真实操作目标不变，隐私模式下禁用重命名。IP 隐藏与隐私仍共用一个开关。
- 添加请求执行期间收到的新分享内容不会被旧请求完成回调清除。

## 0.5.5

### 修复
- 种子列表长距离滑动隐藏底栏后，按手机返回键回到仪表盘时，底部导航保持可见；滚动隐藏仅作用于种子页，不再影响仪表盘和设置。
- 忽略已离开前台的种子页残余滑动回调，防止返回转场期间的惯性滚动再次隐藏目标页面底栏。

## 0.5.4

### 修复
- 种子操作栏和底部导航共用一条高度动画，收起过程中同步释放空间给列表，避免先出现空白黑块、动画结束才补上种子。
- 系统导航栏区域保持稳定，嵌套页面不再重复应用系统边距；滑到列表边缘后的无效滑动不再隐藏底栏。

### 优化
- 所有内置主题名称与简介接入全部支持语言的翻译；保留项目专有名称、原有配色和已保存的主题选择。

## 0.5.3

### 优化
- 再次打开应用时恢复上次主页面（仪表盘、客户端或设置）；分享种子和小组件操作仍优先打开指定页面。
- 记住服务器统计、Tracker 明细及各实例卡片的展开/折叠状态，切换页面和重启应用后保留。

### 修复
- 等待已保存偏好加载后再显示界面，避免开启隐私模式时短暂显示未隐藏内容；IP 隐藏与隐私模式继续共用一个设置。
- 隐私和展开开关改为原子更新，避免快速连续点击丢失状态变化。

## 0.5.2

### 修复
- 仪表盘实例卡片的刷新状态与最后更新时间共用固定单行，避免定时刷新时卡片高度变化、页面上下跳动。

## 0.5.1

### 优化
- 详情页并行加载数据，切换标签时取消过期刷新；速度、进度和 Peer 保持原刷新间隔，
  WebSeed 数据短暂缓存。
- 仪表盘逐实例显示结果，标注最后成功刷新时间；服务器异常时提示统计不完整。
- 详情操作与限速切换显示执行中、成功和失败原因，并阻止重复提交。

### 修复
- 切换实例或查询后丢弃迟到的列表与元数据响应，保留更新的数据流结果；加载更多只发起一次列表请求。
- 统一视图正确显示剩余空间为零的磁盘。
- 网络层保留协程取消语义，不再把取消请求当成网络失败。

### 发布与升级
- 使用固定发布密钥，CI 校验证书、versionCode、标签和双语日志；手动发布构建指定标签。
- **从 v0.5.0 或更早版本升级需要重新安装一次**：旧版本使用临时 debug 密钥。
  卸载会清除应用数据，请先保存服务器连接配置。v0.5.1 起使用同一固定密钥，后续可覆盖升级。

## 0.5.0

### 新增
- 仪表盘新增可折叠的“服务器统计”卡片，可查看各实例的本次/累计传输量、分享率和节点数。
- 内容文件可按名称、大小、进度和优先级排序，默认方向及切换规则与 qui 当前版本一致。
- 新增繁体中文（`zh-TW`），应用重新与 qui 的十种语言保持一致。

### 修复
- I2P 节点会显示规范的 `.b32.i2p` 地址，不再错误显示为 `:0`；旧版响应仍可回退显示
  IPv4 或 IPv6 地址。

## 0.4.5

### 新增
- **设置 → 小部件 → 后台刷新**:关闭 / 15 / 30 / 60 分钟。平台自带的小部件更新计划最低只到
  30 分钟且无法调低,所以小部件不再使用它——`updatePeriodMillis` 设为 0,改由 WorkManager
  任务驱动,15 分钟是安卓对周期性后台任务强制的下限。
- 小部件会说清楚刷新失败的原因——没有网络、凭据被拒绝、服务器超时、HTTP 502——而不是把所有
  情况都报成"无法连接服务器"。
- 点刷新会立刻显示**刷新中…**,按钮不再像是没反应。

### 修复
- 应用长时间没打开后,小部件会一直提示无法连接服务器,点刷新也没用。原因是请求跑在广播接收器
  里,而接收器大约只有十秒就会被系统判定为卡死——一个冷启动的进程要建依赖图、读 DataStore、
  解析 DNS、完成 TLS 握手,然后才轮到请求,根本来不及。现在改到一个没有时限的后台任务里执行,
  它会等网络就绪而不是没网就失败,并且会先重试一次再显示错误。
- 两个小部件同时刷新时会互相拖到超时:第二个的时间预算全花在等第一个的请求上。现在它直接复用
  第一个的结果。
- 桌面上最后一个小部件被移除后,后台刷新计划会停掉。

## 0.4.4

### 新增
- README 和更新日志都有了简体中文版本。GitHub 的 release 说明现在中英双语一起发布,应用的
  更新弹窗按设备语言挑选对应的那一半显示。

## 0.4.3

### 新增
- 合并响应里少了某个没能应答的客户端时,种子列表上会出现一条警告。在此之前那个列表只是悄悄
  变短。
- 应用启动时检查是否有新版本,并显示更新内容。可以「跳过此版本」只压制某一个版本,也可以在
  **设置 → 关于** 里关掉「启动时检查更新」。
- **设置 → 关于** 里显示可用更新的更新内容,而不再只有一个版本号。

### 修复
- 从**设置 → 小部件**添加小部件在某些桌面上毫无反应:MIUI 和澎湃 OS 把「创建桌面快捷方式」
  作为单独的权限,没授权时请求会被静默丢弃。现在会等桌面确认小部件真的放上去了,如果一直没有
  回应,就说明权限在哪里,并给出长按桌面添加的替代路径。
- GitHub 上的 release 完全没有说明,因为自动生成器列的是已合并的 PR,而这个仓库一个都没有。

## 0.4.2

### 修复
- 统一视图头部显示几千个种子,列表却是空的。qui 把跨客户端的元数据序列化成 `instance_id` /
  `instance_name`,而应用只读了驼峰写法,于是合并列表里每一行的实例 ID 都是 null。这让每行的
  key 从 `<实例ID>:<hash>` 变成了裸 hash,于是第一个改变页面顺序的流帧就一个都匹配不上,列表
  被清空。同一个 null 还让点击某一行跳到了当时选中的那个客户端,并且让统一视图下的批量操作
  完全没有目标。
- 用 API 密钥登录必定失败。密钥是拿 `/api/auth/me` 验证的,而那个接口只看 session,对一个完全
  正确的密钥也返回 401。
- 种子列表在后台放一阵子回来后就不动了,要切一下客户端才恢复。SSE 连接没有设读超时,所以进程被
  冻结期间被掐掉的 socket 永远不会抛异常,重连循环也就从不执行。

### 变更
- 统一视图的剩余空间会等列表先到,结果缓存一分钟,不再和第一页抢服务器。
- 仪表盘和详情页的轮询在应用退到后台时暂停。

## 0.4.1

### 修复
- 统一视图的剩余空间显示的是某一个客户端的盘。合并列表背后没有单一的服务器;头部现在显示各
  客户端里最小的那个剩余空间,标上 `≥`,点击展开每个客户端的明细。

### 新增
- **设置 → 小部件**:不经过桌面的选择器直接添加小部件、把小部件锁定到单个客户端、选择传输列表
  优先显示什么。
- 统计小部件在选择器里按三种尺寸(2×1、2×2、4×2)分别列出,而不是一个可拉伸的条目。
- 小部件上的种子名称跟随应用的隐身模式开关。

## 0.4.0

### 新增
- 两个桌面小部件,取代原来那个固定尺寸的卡片:一个可拉伸的统计小部件,以及一个 4×4 的传输列表,
  带进度条,点击某一行可打开对应的种子。

## 0.3.1

### 修复
- 切换到统一视图后,导航栏仍然显示上一次选择的客户端。
- 极简模式下,左划的操作圆钮会露到卡片外面。
- 左划打开的卡片现在会在点击别处或滚动时复位。
- 种子详情页的操作按钮原本只有图标,现在带文字。
- Tracker 明细加上了表头。

## 0.3.0

### 新增
- 跨客户端统一视图、分享限制、带 qui 通配符与模糊语法说明的搜索面板、卡片左划操作、随滚动
  隐藏的工具栏与回到顶部、更新检测、缓存清理,以及第一个桌面小部件。

## 0.2.0

### 新增
- qui 提供的全部九种语言,自动匹配设备语言,可在设置里覆盖。
- 仪表盘的 Tracker 明细和全局统计卡片、IP 隐藏开关,以及可配置的板块显示。

## 0.1.0

首个版本:种子列表、筛选、排序、详情页、添加种子、qui 的全部九套主题,以及隐身模式。
